---
title: "Claude Code can Design Now (Figma is officially in trouble) - 结构化文稿"
channel: "Riley Brown"
published: "2026-03-09"
source_url: "https://www.youtube.com/watch?v=KAX0cWk1fJE"
video_id: "KAX0cWk1fJE"
variant: "structured"
---

# Claude Code can Design Now (Figma is officially in trouble) - 结构化文稿

- **Channel:** Riley Brown
- **Published:** 2026-03-09
- **Source:** https://www.youtube.com/watch?v=KAX0cWk1fJE
- **Main:** [Claude Code can Design Now (Figma is officially in trouble)](2026-03-09-claude-code-can-design-now-(figma-is-officially-in-trouble)-KAX0cWk1fJE.md)

# Claude Code 携手 Paper 挑战 Figma：我用 AI 从零设计并部署了一个网站

今天，我们将要使用 **Claude Code** 来在 Figma 上设计我的个人网站。等等，不对，不是 Figma。我们实际上会使用另一个工具，一个专门为 AI 智能体构建的工具，它叫做 **Paper**。很多设计师告诉我，他们正在从 Figma 切换到 Paper，因为用它配合像 **Claude Code** 和 OpenClaw 这样的 AI 智能体更容易操作。

所以，在这个视频中，我们将：

1. 下载 Paper。
1. 将 Paper 连接到 Claude Code。
1. 使用 Claude Code 来设计我们的网站。
1. 将这个设计变成一个真正的网页，供我使用并发布到互联网上。
这个频道的主要内容是关于如何使用 AI 智能体来有效运营你的业务。如果你喜欢这类视频，我每周会制作好几期，可以考虑订阅一下。让我们开始吧。

## 准备工作：连接两大工具

好的，为了实现这个工作流程，我们需要两个工具：**Claude Code** 和 **Paper**。

你可以在 `paper.design` 找到 Paper。访问 `paper.design` 网站，我们下载 Paper 客户端。这会触发一个下载。选择下载好的文件，我们将它拖入“应用程序”文件夹。然后，在聚焦搜索中输入 `Paper open` 来打开它。现在，Paper 已经在我们的桌面打开了。

当你第一次打开 Paper，会看到这个界面。如果你熟悉 Figma，这个界面看起来会非常像。为了能够控制 Paper，我接下来要打开 **Cursor**（编辑器）。我按下 `Command + Shift + N` 创建一个新项目，然后点击“打开项目”。新建一个文件夹，我把它命名为 `control-paper`。

顺便说一下，我喜欢在 Cursor 里面使用 Claude Code，这只是我的个人习惯。我不知道这是不是最好的方法，但我通常就是在 Cursor 里打开一个终端。我要做的是在终端中输入 `claude`，使用 `--dangerously-skip-permissions` 参数绕过权限检查，并且我们将使用 `--high-effort` 模式。

就这样，我打开了 Claude Code，使用了绕过权限的模式。然后我输入指令，对 Claude Code 说：

> 好的，我希望你搜索互联网，找出如何设置 **Paper.design** 的 MCP 服务器，这样你就可以控制我的 Paper 设计，并创建和编辑设计。完成整个过程。完成后不要问我。我希望你告诉我，你是否可以控制我目前在 Paper 中看到的画板，并在上面添加一个写着 "hello" 的简单文本。
所以，它应该能看到这个画板。我现在就运行这个指令。

我让它去设置 MCP 服务器。如果你不知道那是什么，不用担心，Claude Code 知道。这基本上就是在 Claude Code 和我们打开的 Paper.design 之间建立一个连接。这样它就应该能读取并看到这个画板了。

好的，它说 Paper 已安装。MCP 正在端口 29979 上运行，但返回了 404 状态码。看起来像是失败了。我们看看它能不能自己解决。

哦，我忘了，一旦它设置好 MCP 服务器，需要重置一下才能使用。所以我打算清除并新建一个会话。我输入 `clear` 然后重新开始，并且对它说：

> 你能在我的 Paper 画板上添加 "hello" 吗？
好的，它说完成了。它已经添加了一个新的画板，上面写着 "hello"，并且文字是居中的。让我们回到 Paper。看！出来了。为了再确认一下，我说把它改成 "Hello, I'm Claude." 我们稍等片刻，等它更新。看！搞定了。"Hello, I'm Claude."。

然后我告诉它：

> 请删除此页面上的所有内容。让我们从头开始。我要设计一个很棒的個人網站，现在请清除所有内容。
现在它应该清除了所有东西。看，没错。

## 灵感与指令：开始设计个人网站

好的，这是我想要创建的网站。我在 Twitter/X 上看到了这个设计，我非常喜欢它，所以这将是我的灵感来源。

我的想法是：这只是一个关于我的智能体团队的個人頁面，也就是我的 OpenClaw 智能体团队。

- 左侧面板将是我的 OpenClaw 智能体列表。
- 右侧将显示这些智能体的信息。
- 每个智能体在右侧的信息区域应该与第二张图片相匹配。
- 这张图片只是为了展示我希望在页面右侧包含什么内容。
- 页面的左侧应该是一个智能体列表，即左侧面板。
好的，现在我要做的就是——我刚刚把这些写了下来——然后回到 Cursor。我对它说：

> 好的，我把所有信息都放在这里了。请根据我写的內容創建设计，并使用这两张图片作为输入，因为一张是我希望你模仿的设计，另一张则展示了我想为每个智能体呈现的信息。
（视频插播：简单介绍 `vibecode.dev` 平台）

快速插播一下，聊聊 `vibecode.dev`，这是用 AI 构建专业移动应用并将其发布到应用商店的最简单方式，100% 通过 AI 完成。

这是我创建的一个应用，它让我可以真正地和我的 OpenClaw 对话。我大概花了 30 分钟就用 `vibecode.dev` 创建了这个应用。我也可以使用这个设计工具。我可以直接选择一个元素，比如说我想把这个改成不同的颜色，我们可以选中这个，也可以选中那个，然后说：“请把这些颜色调深一些”。

在它运行的同时，我可以直接添加任何 API。任何你可能想添加到应用里的热门 AI 工具，你都可以直接来这里选择。比如“请实现 Nano Banana”，我们可以把这个指令排进队列。只需点击一下，你就可以添加数据库。你甚至可以添加支付功能，并直接集成到 RevenueCat。当你完成时，点击部署，只需几次点击就能将应用部署到应用商店。你所要做的就是连接你的苹果开发者账号，它会直接将其发布到应用商店。你现在就可以在 `vibecode.dev` 尝试一下。

让我们回到视频正题。

## AI 实时创作：见证设计逐步成形

所以，它应该知道该怎么做。它应该会去查看 Paper。你可以实时看到它的操作。看这个！你可以看到它正在“读取”这两个设计。这就是我非常喜欢这个 MCP 服务器的原因——它在使用东西的时候会有动画效果。让我们看看它做得怎么样。

好的，它在这里创建了这部分，风格和这个（指灵感图）有点像。再看看它还创建了什么：YouTube 智能体、社交媒体智能体、研究智能体、内容写手、代码助手。这太不可思议了。哇，这太疯狂了，这正是我想要的。是的！哦，这是什么？目标？太棒了！这太疯狂了。我的天，这真是太酷了。

我继续对它说：

> 请再创建一个完全相同的副本，但标题和正文都使用 **Instrument Sans** 字体。如果你没有这个字体，请下载它。把它放在当前版本的旁边。
好的，看起来它创建了这个版本。我觉得我更喜欢这个字体一点。这真的很酷。

我又提出一个新想法：

> 现在，在右侧创建一个新版本。在智能体上方添加一个名为“学习”的新版块，里面有一个名为“什么是 OpenClaw”的页面。这是一个介绍什么是 OpenClaw 的学习页面，要让它有视觉效果，并且应该显示在右侧。
它正在创建这个新页面。它正在添加我的智能体。哇，这太疯狂了，伙计。太棒了。太好用了。AI 生成的设计，嗯？真的很有趣。好的，哇，看起来不错。

我继续细化指令：

> 好的，现在我希望你把学习版块的内容做成简单的项目符号列表。智能体那边，请为它们使用更好的图标，比如社交媒体就用更匹配的 Logo。基本上，让那些 Logo 更好看一些。
我刚添加了这个后续指令。然后：

> 接着，我想让你在“OpenClaw 智能体 2”这个画板上进行迭代。我希望你创造一种更新、更酷的方式来展示技能，减少文字量。为整个画板制作三个不同的版本，展示不同的选项。
我们缩小画面，等待它继续工作。好的，它们在这里。这是它正在处理的三个版本。它正在同时处理所有这些。好的，它刚移除了技能部分。我们看看它会怎么做。好的，这相当酷：技能：缩略图生成器、Gemini API、SER API、Supabase、Catala、Google Workspace。实际上，这非常酷。哦，所以这是一个新版本。这是我们可以做的另一个可能版本。在我看来，它和左侧面板太像了。我更喜欢这些。好的，它这里有这个条。我不知道这些条是做什么用的，但我确实喜欢它。这些都是不错的选择。

## 迭代与优化：微调设计细节

所以，我想我喜欢版本 1。我们可以直接告诉它：

> 我喜欢 V1。我们把 V1 移下来。我想在这个设计上进行迭代。你能在这个版本上添加“学习”版块吗？
对，它没有我们之前做的那个“学习”版块。现在它正在工作。搞定了。它添加了“学习”版块。现在它又再次添加了智能体。这看起来很不错。

接着，我想尝试一个更复杂的功能：

> 现在，你能添加一个设置模块吗？当我点击右侧的东西时，它会弹出来。里面应该有一个“启用编辑模式”选项，需要密码，这样管理员就可以编辑网站上的数据并添加新的智能体。
等等，我稍微打断一下这个操作。我希望这个是一个新页面，放在这个页面的右边，就像一个全新的画板。

好的，开始。设置模态框。这太棒了。这真的太酷了。我被这个震撼到了。哦，是的。好的，就是这样。看起来很不错。密码输入框……哦，哇，这太牛了。哦，它还添加了暗黑模式和通知。

我们得克制一点。第一个模态框不需要暗黑模式，也不需要那些其他选项。它不应该显示“Riley Brown”，而应该显示“设置”，因为没有登录功能，只有编辑模式的密码。把前面两个画板上的这个都改一下。

好的，它应该会编辑这两个。这里应该只显示“设置”。它已经在这里编辑好了。也应该把它加在这里。好了。我跟它说：

> 把“R”图标换成设置的齿轮图标。
这样看起来就很不错了。

## 从设计到代码：一键生成可部署网站

现在，我们要开始一个新的会话。我打算对它说：

> 好的，我喜欢剩下的这三个设计。我想用它们来构建一个网站。请使用它们，并根据这些设计构建完整的网站。不要添加编辑模式，只需创建前端界面和所有智能体的信息等。
好的，让我们看看这个能不能行。我以前从没试过这个，但我很确定你可以做到。你可以看到它正在读取这些页面，对吧？这是我们正在使用的三个设计。这应该会生成一个相当简单的网站。让我们看看是否可行。

好的，看起来它完成了。我们有了一个 `app` 文件夹。看起来它把它变成了一个 React 应用。我们可以去终端。它在这里打开了，地址是 `localhost:5173`。你可以看到这里我们有“远程设置”等，所有东西看起来和设计稿非常接近了。如果我们回到 Paper，我们可以看到这里看起来非常相似。就是这样。

我们基本上使用了 **Claude Code**（我在 Cursor 里做的），我们能够像这样在 Paper 上创建设计。我们进行了迭代，找到了我们喜欢的风格，然后把它变成了一个 React 应用。

## 最后一步：部署上线

我认为最后一步是：

> 请问你能使用 CLI 把这个推送到 GitHub 和 Vercel 吗？
我喜欢这样做：我去 GitHub，创建一个新的公共仓库，就叫 `landing-page-design-test`。确认一下我的名字是否正确。复制仓库地址。

现在我们要做的是回到 Cursor，对它说：

> 这是仓库地址，使用它，然后推送到 Vercel，给我公开链接。这样就会真正把这个网站放到互联网上，之后我们可以添加自定义域名。这基本上是最后一步了。
我们等它完成。看起来它已经完成了。如果我们点击这个链接，应该能看到仓库里已经有了所有内容。如果我们回到 Cursor，我们应该会得到一个打开的公共链接。看！搞定。网站已经在互联网上了。你可以访问这个网站，这就是我们刚才创建的设计。

是的，我们用 **Claude Code** 设计了它，并用 Claude Code 轻松地创建了一个网站，通过 Vercel 把它放到了互联网上。就这样。

你知道吗，这其实是我第二次使用这个工具，所以我还会继续深入研究，可能会在接下来的几周里制作一个更深入的指南。我可能会真的使用 OpenClaw，并加入其他技能，比如能够抓取互联网，直接将图片添加进去；或者如果你想生成像商业报告那样的东西，使用 Paper 自动抓取公司 Logo。我脑子里冒出了很多有趣的想法。这个视频算是展示了一下基础操作。我会在使用这个工具上变得更熟练，然后再做另一个视频。

谢谢大家的观看。
